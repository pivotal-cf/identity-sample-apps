---
server:
  port: 8080

logging.level:
  org.springframework.security: DEBUG

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: "https://demo.login.run.pivotal.io/token_keys"

sso:
  issuer: "https://demo.uaa.run.pivotal.io/oauth/token"
  audience: "foo,todo"
