---
server:
  port: 8080

logging.level:
  org.springframework.security: DEBUG

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: "${AUTH_SERVER}/token_keys"

sso:
  issuer: "${AUTH_SERVER}/oauth/token"
  audience: "foo,todo"
