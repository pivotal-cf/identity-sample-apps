---
server:
  port: 8889

logging.level:
  org.springframework.security: DEBUG

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: "https://demo.login.run.pivotal.io/token_keys"
          # what about issuer check?

ssoServiceUrl: "https://demo.login.run.pivotal.io"